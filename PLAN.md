# HQ

## Next Up

- **[usability/medium] Make conversation readiness, command outcomes, and self-authored messages visually self-evident** — Refine the ordinary conversation transcript without discarding typed lifecycle evidence. Omit successful agent-turn rows while retaining those entries in conversation state so running-to-terminal detection and automatic continuation-draft opening still work; keep failed and interrupted turns visible. Always show an explicit `Agent is working…` indicator for typed running work, with nonempty progress text presented in addition to rather than instead of that readiness signal, unless a visible interaction is awaiting the user's input. Apply the same visibility rules to rendering, viewport geometry, tail following, entry navigation, selection, and technical-detail routing so hidden terminal entries cannot become invisible anchors. For structured commands, preserve the command, syntax highlighting, bounded output preview, omission cues, and technical details, but omit the successful completion/exit-zero line; append `Exited with code N` beneath failed command output in `conversation.activity.error`, use a generic error footer when failure has no exit code, and keep interrupted commands identifiable without deriving state from summary prose. Render locally authored messages as persistent full-width brighter/inverted semantic bands, omit the `You` label, retain surrounding whitespace, and show only useful delivery or message-state labels such as `Pending`, `Received`, `Archived`, or `Could not be delivered`. Define the self-message surface coherently across built-in, no-color, Base16, and configurable native themes, including legible Markdown and focused/unfocused selection, and document its theme role. Cover running and terminal agent turns, command exit variants and footer order/color, self-message width/spacing/content at narrow and wide sizes, selection/navigation around hidden entries, and end-to-end readiness behavior; update PTY expectations that currently wait for `You · Pending` or `Agent finished` to wait on meaningful message/readiness state while proving the working indicator appears before completion. (`crates/hq-tui/src/render.rs`, `crates/hq-tui/src/model.rs`, `crates/hq-tui/src/theme.rs`, `docs/tui-themes.md`, `crates/hq-tui/tests/render_snapshots.rs`, `crates/hq-node/tests/unix_tui_terminal.rs`)
- **[operational/medium] Terminate the complete Codex process tree on forced shutdown** — Codex app-server processes have no isolated process group and forced cleanup kills only the direct child, allowing tool subprocesses to survive after HQ reports the runtime stopped. Give each runtime a controllable process tree and verify abnormal shutdown removes descendants without affecting unrelated processes. (`crates/hq-codex/src/process.rs:142`)
- **Add familiar character and word movement shortcuts to TUI text editing** — Normalize Ctrl-B/Ctrl-F as character-backward/character-forward and Ctrl-Left/Ctrl-Right as word-backward/word-forward, represent them as editing-specific `UiInput` intents, and apply them to every existing cursor-capable text surface: message drafts, searches, agent and session names, and project forms. Define a word as a maximal run of Unicode alphanumeric characters plus `_`; backward movement skips separators and lands at the previous word's start, while forward movement crosses the current word and separators to the next word's start or the text end, with punctuation, whitespace, and newlines treated as separators. Preserve UTF-8 boundaries; make beginning/end movement a no-op; do not change text, dirty state, validation errors, or autosave scheduling; keep plain Left/Right and Ctrl-C behavior unchanged; and make these editing-only chords inert outside editable fields. Complete the work with crossterm normalization tests, reusable-editor tests covering punctuation, whitespace, newlines, and multibyte text in single- and multiline fields, plus updated contextual editing help. Append/backspace-only interaction answers and configuration editing remain out of scope. (`crates/hq-node/src/tui_shell.rs:514`, `crates/hq-tui/src/model.rs:758`, `crates/hq-tui/src/model.rs:9459`, `crates/hq-tui/src/render.rs:589`)
